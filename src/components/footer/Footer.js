import React from 'react'
import styles from './footer.module.css'

const Footer = () => {
  return (
    <div className={`${styles.container}`}>
        <p className={`${styles.item}`}>Juego para aprender Colores RGBA</p>
    </div>
  )
}

export default Footer